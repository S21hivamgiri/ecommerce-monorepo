# Root package. Domain boundaries are declared here so every service's BUILD file can reference them.
package_group(
    name = "all_services",
    packages = ["//gateway/..."],
)


load("@npm//:defs.bzl", "npm_link_all_packages")

npm_link_all_packages(name = "node_modules")